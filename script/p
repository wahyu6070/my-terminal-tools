#!/usr/bin/env bash

# Perbarui lastmod lalu sinkronkan dengan remote secara aman.
# Argumen pertama menjadi pesan commit; default-nya "p".
SCRIPT_DIR="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd)"
exec bash "$SCRIPT_DIR/git-sync" "${1:-p}" "./content/game" "21600"
